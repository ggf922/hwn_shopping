/**
 * ============================================================================
 * 데이터베이스 진입점 (호환성 shim)
 * ============================================================================
 * 기존 코드가 `require('./db')` 로 임포트하던 SQLite 인스턴스 대신,
 * 새로운 어댑터 모듈을 노출합니다.
 *
 * - 환경변수 USE_SUPABASE=true → Supabase 어댑터
 * - 그 외 → SQLite 어댑터 (기존 동작 유지)
 *
 * server.js 는 이제 db.products, db.vouchers, db.orders, db.voucherAmounts 형태로 사용.
 * ============================================================================
 */

module.exports = require('./db/index');
